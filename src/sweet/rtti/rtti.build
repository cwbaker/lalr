
local settings = build.current_settings();
for _, architecture in ipairs(settings.architectures) do
    build.Library ("rtti", architecture) {
        libraries = {
            "sweet/assert/assert"
        };

        build.Cxx (architecture) {
            "Type.cpp",
            "SweetTypeInfo.cpp"
        };
    };
end
