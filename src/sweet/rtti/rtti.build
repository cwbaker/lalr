
for _, build in build:default_builds() do
    build:Library "rtti" {
        libraries = {
            "sweet/assert/assert"
        };

        build:Cxx () {
            "Type.cpp",
            "SweetTypeInfo.cpp"
        };
    };
end
