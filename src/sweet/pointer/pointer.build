
buildfile "pointer_test/pointer_test.build";

for _, build in build:default_builds() do
    build:Library "pointer" {
        libraries = {
            "sweet/assert/assert",
            "sweet/error/error"
        };

        build:Cxx () {
            precompiled_header = "stdafx.hpp";
            "ReferenceCount.cpp"
        };
    };
end
