
for _, cc in toolsets('^cc.*') do    
    cc:all {
        cc:Executable '${bin}/lalrc' {
            '${lib}/lalr_${platform}_${architecture}';
            cc:Cxx '${obj}/%1' {
                'dot.cpp';
                'lalrc.cpp';
            };
        };
    };
end
